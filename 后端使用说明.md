# 尼尔号后端使用说明

> **完整资源包 / 前端下载**：请到哔哩哔哩搜索 **尼奥开源计划**，按合集说明下载完整客户端资源（含 `NieoData/`）。本仓库仅为 Go 后端，不含前端资源。

## 1. 启动前准备

| 项 | 说明 |
|----|------|
| Go | 已安装，可执行 `go` |
| MySQL（可选） | 默认 `root` / `root`，库名 `niao`；**连不上会自动用 JSON 存档**（`server/data/saves/`） |
| 客户端资源 | 将 B 站「尼奥开源计划」下载的完整资源放到与 `server` 同级，目录名为 `NieoData/`（含 `dll/`） |

本机默认：`configs/advertise_host.txt` 写 `127.0.0.1`。局域网/公网开服改 IP，见 `NieoData/config/ServerR-开服说明.txt`。

## 2. 一键启动

```bat
cd server
build.bat
start.bat
```

- `build.bat`：编译到 `output/`，再覆盖 `bin/nieoserver.exe`
- `start.bat`：改 `ServerR.xml` 登录地址 → 同步 DLL → 启动服务

启动成功后控制台会打印类似：

```
[run] GM  http://127.0.0.1:29180/
[run] RES http://127.0.0.1:41520/
[store] backend=mysql ...   （或 backend=json ...）
```

### 端口一览

| 用途 | 端口 | 打开方式 |
|------|------|----------|
| 游戏网页（资源服） | **41520** | 浏览器打开客户端 |
| GM 后台 | **29180** | 浏览器打开管理页 |
| 登录 TCP | 22973 | 客户端自动连 |
| 游戏 TCP | 28610 | 登录成功后自动连 |

## 3. 打开游戏网页

浏览器访问：

```
http://127.0.0.1:41520/
```

局域网其它电脑把 `127.0.0.1` 换成开服机器 IP（且 `advertise_host.txt` 也要是该 IP，并重启 `start.bat`）。

需要 Flash 运行环境（PPAPI / 旧浏览器等，按你本机已有方案）。

## 4. 创建游戏账号（客户端）

本服登录 **104**：邮箱不存在则 **自动注册**，无需单独注册页。

1. 打开 `http://127.0.0.1:41520/`
2. 用任意邮箱 + 密码登录（例如 `test@niao.local` / `123456`）
3. 首次会进创角（协议 **108**）：起昵称、选形象
4. 之后选服进游戏（**105** → 游戏服 **1001**）

注意：

- 密码按客户端规则以 MD5 入库；同一邮箱再登录须用原密码，否则报错
- 已创角账号不会再走创角流程

### 命令行快速测登录链（可选）

```bat
cd server
go run ./cmd/logintest
```

默认邮箱 `test@niao.local`、密码 `123456`。也可：

```bat
go run ./cmd/logintest 127.0.0.1:22973 你的邮箱@xx.com
```

（密码仍默认 `123456`。）

## 5. GM 后台

地址：

```
http://127.0.0.1:29180/
```

默认账号（`configs/gm_auth.json`）：

- 用户名：`admin`
- 密码：`admin`

登录后常用页：

| 页面 | 作用 |
|------|------|
| `/` 或发放页 | 发赛尔豆/金豆/积累经验、道具、精灵、新手礼包 |
| `/accounts.html` | 搜玩家（UID / 昵称 / 邮箱）、封禁 |
| `/user.html?uid=` | 账号详情、改昵称等 |
| `/online.html` | 在线列表、踢人 |
| `/mail.html` | 邮件相关 |
| `/pets.html` / `/items.html` | 精灵 / 道具侧管理 |
| `/pet-stats.html?uid=&catchTime=` | 直接改精灵**面板能力值**（持久覆盖；可一键恢复正常） |
| `/audit.html` | 操作审计 |
| `/gm-accounts.html` | GM 账号（规划/扩展页） |

改 GM 密码：编辑 `configs/gm_auth.json` 后重启服务。

## 6. 换 IP / 给别人连

1. `configs/advertise_host.txt` 改成开服机 IP（一行）
2. 重新 `start.bat`（会自动改 `NieoData/config/ServerR.xml` 与 `ip.txt`）
3. 对方浏览器打开：`http://开服IP:41520/`
4. 防火墙放行：**41520、22973、28610**（以及需要时的 **29180**）

## 7. 常见问题

| 现象 | 处理 |
|------|------|
| 资源页空白 / DLL IOError | 确认 `NieoData/dll/NieoCore.swf` 存在；再跑一次 `start.bat` |
| 登录连不上 | 看 `ServerR.xml` 五个入口是否为当前主机:`22973`；看控制台 `[login] TCP` |
| 提示 MySQL 失败但仍能开 | 正常，已回落 JSON；数据在 `data/saves/` |
| GM 打不开 | 确认服务在跑，访问 **29180** 不是 41520 |
| README 旧端口 | 以本文 **29180 / 41520** 为准 |

端口与协议以本文及控制台打印为准。
